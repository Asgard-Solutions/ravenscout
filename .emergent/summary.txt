<analysis>Here is the handoff summary based on the latest context and actions.

**original_problem_statement**: 
Finish Hunt-Style Modifiers implementation (archery, rifle, blind, saddle, public land, spot-and-stalk) in the backend LLM prompt pipeline and frontend UI. Fix image-analysis bugs where results were not showing, overlays were misaligned, and the mobile application was crashing or bouncing back to the home screen after processing large map captures. PRODUCT REQUIREMENTS: Provide tactical hunting advice, multi-image context uploads, strictly separate UI/JSON/media payloads, and use reliable coordinate geometry for overlays.

**User's preferred language**: English (INSTRUCT next agent to respond in English only)

**what currently exists?**: 
A full-stack React Native (Expo) mobile app with a FastAPI/MongoDB backend. The app features a v3 split architecture for LLM metadata vs. S3 media persistence. The prompt pipeline handles seasonal, regional, and now hunt-style modifiers. Frontend UI supports drawing scaled, draggable tactical overlays over map screenshots.

**Last working item**: 
    - Last item agent was working: Fixing a Mobile Chrome Out-Of-Memory (OOM) crash caused by tall panoramic screenshots. The agent discovered that  was only compressing based on , allowing 2048x4437 images to decode into ~36MB bitmaps and instantly crash the  page on mobile browsers. The agent fixed  to constrain the *longest* dimension but paused before lowering the   in .
    - Status: IN PROGRESS
    - Agent Testing Done: N (For the final file edit, but 137 frontend tests passed prior)
    - Which testing method agent to use? Frontend testing agent / manual testing
    - User Testing Done: N

**All Pending/In progress Issue list**: 
  Issue 1: Mobile Chrome crashes / unmounts instantly on  for tall panoramic images.
     - Attempted fixes: Added a lightweight  (AsyncStorage) to survive route transitions. Temporarily disabled the background  call in  to reduce duplicate memory allocations.
     - Next debug checklist: Update  to reduce   from 2048 to 1600 to ensure strict memory safety. Verify  stays mounted.
     - Why fix this issue and what will be achieved with the fix? Stops the browser tab from OOM crashing, allowing the user to actually see their analysis results.
     - Status: IN PROGRESS
     - Is recurring issue? Y
     - Should Test frontend/backend/both after fix? Frontend

**In progress Task List**: 
  Task 1: Restore full  persistence (S3 + MongoDB).
     - Where to resume: In  (or inside ), re-introduce the  call. It was temporarily removed to isolate the OOM crash.
     - What will be achieved with this? Right now, analyses are ONLY saved to local . Re-adding this ensures images upload to AWS S3 and the hunt metadata persists to MongoDB.
     - Status: BLOCKED (Wait until the OOM crash is confirmed fixed by the user before re-adding).
     - Should Test frontend/backend/both after fix? Both
     - Blocked on something: Issue 1 (Memory stability).

**Upcoming and Future Tasks**
    - P1: EAS production build for real RevenueCat purchases (Expo Go does not support live RevenueCat).
    - P2: GIS integration for georeferenced overlays (mapping AI outputs to strict Lat/Lon coordinates so MapLibre can pan to them).
    - P2: Trail cam integration.
    - P3: Predictive movement modeling.
    - P3: Wind shift notifications for stands (premium feature).
    - P3: Background orphan asset detection/cleanup for local device storage.

**Completed work in this session**
- Hunt-style Modifiers implemented across backend prompt packs (, , ) and frontend UI picker () (DONE)
- Fixed major overlay misalignment by creating , changing  to , and mapping LLM coordinates precisely to the fitted image rect (DONE)
- Refactored mobile hydration pipeline via  to survive SSR/runtime refreshes on mobile Chrome (DONE)
- Added  to compress images on upload/capture (DONE)
- Changed default MapLibre style to  (DONE)
- Added fallback  support for  in FastAPI  (DONE)
- Improved backend error handling/messaging when LLM budget limits are exceeded (DONE)

**Earlier issues found/mentioned but not fixed**
   - None explicitly, but  is temporarily stripped from the critical path and must be re-added.

**Known issue recurrence from previous fork**
  - Issue recurrence in previous fork: 0
  - Recurrence count: 0
  - Status: RESOLVED

**Code Architecture**
app/
├── backend/
│   ├── server.py (FastAPI main entrypoint)
│   ├── prompt_builder.py
│   ├── species_prompts/ (Hunt styles added here)
│   └── tests/ (312 passing tests)
└── frontend/
    ├── app/ (results.tsx, setup.tsx)
    └── src/
        ├── media/ (imageProcessor.ts, imageProfiles.ts, provisionalHuntStore.ts, huntHydration.ts)
        ├── components/ (ImageOverlayCanvas.tsx)
        ├── utils/ (imageFit.ts, analysisContext.ts)
        └── __tests__/ (137 passing tests)

**Key Technical Concepts**
React Native (Expo Router), FastAPI, MongoDB, AWS S3, MapLibre GL JS. Strict use of base64 image compression () prior to LLM analysis to protect mobile browser heap memory. 

**key DB schema**
  - MongoDB (): User configurations, hunt metadata.
  - AsyncStorage (): Temporary bridge to pass LLM results safely across mobile browser route unmounts/refreshes.

**changes in tech stack**
  - Heavy reliance on  to enforce strict resolution limits () before putting images into React state.

**All files of reference**
  - : Contains the  logic that needs fixing (just updated to use ).
  - : Contains . Next agent needs to drop  to 1600.
  - : Location where  is called.  needs to be added back here eventually.
  - : New file managing overlay math to prevent drift.
  - : New local storage bridge.

**Areas that need refactoring**: 
  -  pipeline decoupling: Currently it's dangerous to do heavy base64 string manipulation during a route transition on low-memory devices.

**key api endpoints**
  - 
  - 

**Critical Info for New Agent**
  - Do NOT re-introduce the  call into  until the user verifies that the  page stops crashing. Memory OOMs on mobile web are the primary blocker right now.
  - The backend successfully accepts user's custom  and  via the  without modifying protected files.
  - S3 logic works perfectly, but images aren't currently being uploaded because  is detached. 

**documents and test reports created in this job**
  -  (Updated with backend/frontend testing assertions)

**Last 10 User Messages and any pending HUMAN messages** 
 1. (User) app not loading on expo now
 2. (Agent) Used troubleshoot agent, confirmed it was an ngrok race condition, fixed itself.
 3. (User) Still not working and still sending you back to the analyze hunt page. also, when you click on map can we default to the satellite view.
 4. (Agent) Discovered user's Emergent LLM key exceeded the  budget. Fixed error messages and set map to satellite.
 5. (User) I have updated .env to use my LLM key so now we should be good.
 6. (User) still getting results not found
 7. (Agent) Realized  import was missing. Fixed it.
 8. (User) I saw the hunt analysis page load a second then it went back to new hunt page. still no images in S3 and I dont see anyting the mongo
 9. (Agent) Realized background  was OOM crashing the browser tab by duplicating 1.35MB payloads. Removed  temporarily.
 10. (User) It loaded for a split second then went back to the new hunt screen. (Agent realized panoramic screenshots weren't being constrained by height, causing 36MB bitmap memory spikes).

**Project Health Check:**
  - Broken: Mobile Chrome Expo Go preview crashes via OOM when displaying tall, high-res base64 screenshots.
  - Mocked: RevenueCat.

**3rd Party Integrations**
  - OpenAI GPT-5.2 Vision — uses User LLM Key (Switched from Emergent Key due to budget cap)
  - WeatherAPI.com — requires User API Key
  - MapTiler — requires User API Key
  - RevenueCat — requires User API Key
  - AWS S3 — uses User AWS Credentials

**Testing status**
  - Testing agent used after significant changes: YES (deep_testing_backend_v2 run)
  - Troubleshoot agent used after agent stuck in loop: YES (Used for Ngrok tunnel diagnostic)
  - Test files created: , , 
  - Known regressions: S3 upload and Mongo persistence disabled temporarily.

**Credentials to test flow:**
  - See 

**What agent forgot to execute**
The agent modified  to properly identify the longest edge of an image (fixing the panoramic bug), but it forgot to actually reduce the   threshold down to  inside  before the session ended.

**Debugging checklist for current issue**
    Issue: Memory Crash (OOM) on  for tall screenshots
    Frontend files to check: 
    -  (Lower  maxDim to 1600)
    -  (Ensure  remains disabled until the crash stops happening)
    -  (Ensure the  component is safely garbage collecting old bitmaps)</analysis>
